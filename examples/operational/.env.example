# DSS Operational Examples - Environment Configuration
# Copy this file to .env and fill in your values

# ============ Network Configuration ============
# Ethereum RPC URL (Infura, Alchemy, or your own node)
RPC_URL=https://mainnet.infura.io/v3/YOUR_INFURA_KEY

# Chain ID (1 = Mainnet, 5 = Goerli, 11155111 = Sepolia)
CHAIN_ID=1

# ============ Wallet Configuration ============
# Private key for bot operations (NEVER commit this!)
# For production, use hardware wallet or KMS
PRIVATE_KEY=0x...

# Multi-sig address for governance operations
MULTISIG_ADDRESS=0x...

# Timelock contract address (for upgrades)
TIMELOCK_ADDRESS=0x...

# ============ Strategy Configuration ============
# Your DSS strategy contract address
STRATEGY_ADDRESS=0x...

# Strategy ABI path (relative to operational/ directory)
STRATEGY_ABI_PATH=../sdk/basic-strategy/artifacts/contracts/HODLStrategy.sol/HODLStrategy.json

# ============ OpenZeppelin Defender ============
# Get these from https://defender.openzeppelin.com/
DEFENDER_API_KEY=your_defender_api_key_here
DEFENDER_API_SECRET=your_defender_api_secret_here

# Defender Team ID (optional)
DEFENDER_TEAM_ID=your_team_id

# ============ Tenderly Configuration ============
# Get these from https://dashboard.tenderly.co/
TENDERLY_ACCESS_KEY=your_tenderly_access_key
TENDERLY_ACCOUNT_SLUG=your_account_name
TENDERLY_PROJECT_SLUG=your_project_name

# ============ Alerting Configuration ============
# Slack webhook for alerts
SLACK_WEBHOOK_URL=https://hooks.slack.com/services/YOUR/WEBHOOK/URL

# Telegram bot for alerts
TELEGRAM_BOT_TOKEN=your_telegram_bot_token
TELEGRAM_CHAT_ID=your_telegram_chat_id

# Email alerts (using SendGrid or similar)
SENDGRID_API_KEY=your_sendgrid_api_key
ALERT_EMAIL=alerts@yourdomain.com

# Discord webhook for alerts
DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/YOUR/WEBHOOK

# ============ Monitoring Configuration ============
# Health check interval (milliseconds)
HEALTH_CHECK_INTERVAL=60000

# Event monitoring start block (0 = latest)
EVENT_MONITOR_START_BLOCK=0

# Gas price threshold for alerts (gwei)
GAS_PRICE_ALERT_THRESHOLD=100

# ============ Keeper Bot Configuration ============
# Minimum time between rebalances (seconds)
MIN_REBALANCE_INTERVAL=86400

# Gas price limit for keeper operations (gwei)
MAX_GAS_PRICE=150

# Enable dry-run mode (true = don't execute, just log)
DRY_RUN=true

# ============ Deployment Configuration ============
# Number of confirmations to wait
DEPLOYMENT_CONFIRMATIONS=5

# Gas limit for deployment
DEPLOYMENT_GAS_LIMIT=5000000

# ============ Logging Configuration ============
# Log level (debug, info, warn, error)
LOG_LEVEL=info

# Log to file (true/false)
LOG_TO_FILE=true

# Log file path
LOG_FILE_PATH=./logs/operational.log

